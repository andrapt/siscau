$(document).ready(function() {

    var btnPesqusiar = $('#btnPesqusiar');
    var formPesquisar = $('#formPesquisar');

    // Handle form submission
    $(btnPesqusiar).on('click', function() {
        alert();
    });
    // $('#search-form').on('submit', function(event) {
    //     event.preventDefault(); // Prevent default form submission
    //     const searchValue = $('#search').val().trim();
        
    //     if (searchValue) {
    //         // Redirect to the same page with the search query
    //         window.location.href = `?table_search=${encodeURIComponent(searchValue)}`;
    //     }
    // });
});